{% extends 'base.html.twig' %}

{% block title %}New Student{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4">Create New Student</h1>

        {{ include('student/_form.html.twig') }}

        <a class="btn btn-light mt-3" href="{{ path('app_student_index') }}">Back to List</a>
    </div>
{% endblock %}
